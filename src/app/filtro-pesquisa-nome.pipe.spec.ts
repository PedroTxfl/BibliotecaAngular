import { FiltroPesquisaNomePipe } from './filtro-pesquisa-nome.pipe';

describe('FiltroPesquisaNomePipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroPesquisaNomePipe();
    expect(pipe).toBeTruthy();
  });
});
