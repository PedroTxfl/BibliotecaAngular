import { SimNaoDisponivelPipe } from './sim-nao-disponivel.pipe';

describe('SimNaoDisponivelPipe', () => {
  it('create an instance', () => {
    const pipe = new SimNaoDisponivelPipe();
    expect(pipe).toBeTruthy();
  });
});
