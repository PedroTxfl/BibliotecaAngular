import { ExtrairNomePipe } from './extrair-nome.pipe';

describe('ExtrairNomePipe', () => {
  it('create an instance', () => {
    const pipe = new ExtrairNomePipe();
    expect(pipe).toBeTruthy();
  });
});
