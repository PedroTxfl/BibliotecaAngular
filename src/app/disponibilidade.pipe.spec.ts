import { DisponibilidadePipe } from './disponibilidade.pipe';

describe('DisponibilidadePipe', () => {
  it('create an instance', () => {
    const pipe = new DisponibilidadePipe();
    expect(pipe).toBeTruthy();
  });
});
